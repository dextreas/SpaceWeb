document.querySelector('.explore-btn')
    .addEventListener('click', function () {
        // Navigate to the next page when the button is clicked
        window.location.href = 'choose.html'; // Specify the filename of your new HTML file
    });